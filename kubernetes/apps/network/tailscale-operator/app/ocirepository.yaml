---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: tailscale-operator
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 1.102.3
  # Upstream publishes to ghcr.io/tailscale/tailscale/tailscale-operator, which
  # is not anonymously pullable. The home-operations mirror is.
  url: oci://ghcr.io/home-operations/charts-mirror/tailscale-operator
