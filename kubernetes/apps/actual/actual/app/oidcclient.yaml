---
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: actual
spec:
  callbackUrls:
    - https://actual.${SECRET_DOMAIN}/openid/callback
  logoutCallbackUrls:
    - https://actual.${SECRET_DOMAIN}
  pkceEnabled: true
  skipConsent: true
  secret:
    name: actual-oidc
    keys:
      clientID: client_id
      clientSecret: client_secret
