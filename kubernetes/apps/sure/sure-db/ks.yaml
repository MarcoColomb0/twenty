---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sure-db
spec:
  dependsOn:
    - name: cloudnative-pg
      namespace: database
    - name: dragonfly-operator
      namespace: database
  interval: 1h
  path: ./kubernetes/apps/sure/sure-db/app
  postBuild:
    substitute:
      APP: sure
      CNPG_BACKUP_SCHEDULE: "0 0 4 * * *"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: sure
  wait: true
